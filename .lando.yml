name: imalabya
recipe: drupal9
config:
  webroot: web
  php: "7.4"
  xdebug: false
services:
  database:
    type: mariadb:10.4
    creds:
      user: database
      password: database
      database: database
